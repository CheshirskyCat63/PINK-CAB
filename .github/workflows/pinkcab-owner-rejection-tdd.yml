name: DEPRECATED - PINK CAB Owner Rejection TDD

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Stop legacy lane
        shell: bash
        run: |
          echo "::error::This workflow is deprecated. Use 'PINK CAB G1 GitHub Control Plane' on fix/CD-848-recovery-r1-input-contract."
          echo "Remote Desktop Commander is not part of the PINK-CAB execution pipeline."
          exit 1
