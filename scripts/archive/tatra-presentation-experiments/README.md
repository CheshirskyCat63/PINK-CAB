# Archived Tatra presentation experiments

These Desktop/Faithful import-export variants are preserved for history only.

The active presentation pipeline is the scene-preserved path in `scripts/export_tatra_scene_preserved.py` and `scripts/import_tatra_scene_preserved.py`, with the validated V12Clean wheel supplied separately.

Do not use these archived scripts as production authority.
